---
name: aps-cli
description: Use this skill when using APS CLI.
---

# APS CLI

## Instructions

- Use `node src/index.ts --help` to inspect available commands.
- Assume the user is already signed in unless they state otherwise. If the CLI returns login required, pause work and ask the user to login.